--- 
bodyClass: home
---
{% extends "wrapper.njk" %}

{% block intro %}
<div class="intro-section">
  <div class="content">
    <h1>{{ site.tagline }}</h1>
    <p>{{ site.description }}</p>
    <a class="action" href="#main">{{ actionText }}</a>
  </div>
</div>
{% endblock %}
