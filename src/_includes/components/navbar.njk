<div class="navbar">
  <div class="content">
    {% include "components/logo.njk" %}
    <nav>
      <ul>
        {% for item in navItems %}
          <li><a href="{{item.href}}">{{item.name}}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</div>
