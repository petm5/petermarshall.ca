<ul class="grid posts">
  {% for post in posts %}
  <li>
    <a href="{{post.url}}">
      <div class="image">
        {%- if post.data.featuredImage %}
        <img src="{{post.data.featuredImage}}" alt="" />
        {% endif -%}
      </div>
      <div class="text">
        <p><strong>{{post.data.title}}</strong></p>
        <p><small class="date">{{post.date | postDate}}</small></p>
      </div>
    </a>
  </li>
  {% endfor %}
</ul>
